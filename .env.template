# Sentinel Command Center — Environment Template
# No API keys are required. All data is served from local mock routes.
# This file is intentionally minimal.

# Host port (container always listens on 3000)
PORT=3000
