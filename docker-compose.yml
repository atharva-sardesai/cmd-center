name: sentinel
services:
  sentinel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sentinel
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
